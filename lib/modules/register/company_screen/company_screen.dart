import 'package:flutter/material.dart';

class LoginCompanyScreen extends StatelessWidget {
  const LoginCompanyScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Login cpmoany',style: TextStyle(fontSize: 19.0),),
      ),
    );
  }
}
